# Mutantenbaeume werden ERZEUGT, nicht eingecheckt: elf Kopien des
# Gut-Musters sind rund 5 MB auf der Platte (464 K Bytes je Baum, 40 kleine
# Dateien), und sie waeren eine zweite Fassung desselben Codes, die beim
# naechsten Fix stillschweigend veraltet.
#
#   bash tests/verify/meta.sh T-7.5      (ruft erzeugen.sh selbst auf)
*
!.gitignore
!erzeugen.sh
